version: 1
backend:
  phases:
    build:
      commands:
        - nuxt build --extends docus
frontend:
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .output
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
